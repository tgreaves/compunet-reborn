# Python
__pycache__/
*.pyc

# OS
Thumbs.db
.DS_Store

# IDE
.vscode/
.idea/

# Generated intermediates
client/chargen

# Build output
client/c64/src/build/

# Connection artifacts
client/c64/127.0.0.1:*

# Secrets
.env

# Live user and server data
server/cfg/users.json
server/cfg/pending.json
server/cfg/subscribers.json

# Dynamic server data (not tracked — use data.example/ for templates)
server/data/mail/
server/data/votes.json
server/data/content/
server/data/partyline.jsonl
server/data/audit.jsonl
server/data/archive/
# ⚠ server/data/mail.test/ is TRACKED, deliberately — it is the mail half of the
# test fixtures, the counterpart to content.test/. Ignoring it meant the suite
# passed here and had nothing to run against on a fresh clone. The tests copy it
# to a temp directory before use, so running them leaves it untouched.

# Virtual environments
server/venv/
website/venv/
client/amiga/vintage/tools/re/.venv/

# Amiga RE generated flat image (regenerate with flatten.py; only the .map is tracked)
client/amiga/vintage/tools/re/compunet_flat.bin

# Website runtime data
website/pending.json

# Logs
server/logs/
website/logs/
client/c64/logs/
*.pid

# Local scripts
transfer-repo.sh
client/c64/src/partyline/build/
