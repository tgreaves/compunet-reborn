# Flattened raw images are generated by flatten.py from the HUNK binaries — large,
# regenerable, and not source. (The .map is small + useful, so it IS tracked.)
*_flat.bin
compunet_flat.bin
editor_flat.bin
