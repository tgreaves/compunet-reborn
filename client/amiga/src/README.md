# Compunet Amiga client — C reconstruction

A from-scratch, **readable and compilable** reconstruction of the original 1989
SAS/Lattice C client, rebuilt module-by-module and verified against the decompiled
reference in [../vintage/tools/re/recon_annotated.c](../vintage/tools/re/recon_annotated.c).

Goal: source that reads like the original (real names, Amiga system includes, real
struct types — not the decompiler's raw offset arithmetic) and compiles for
Kickstart 1.3, so it can eventually be built in SAS/C as it originally was, and so
the transport can be swapped for TCP (Compunet Reborn).

## Coverage — this is not a hand-picked subset

The set of functions reconstructed here is the **auditable application-function
census** in [../vintage/tools/re/coverage-census.md](../vintage/tools/re/coverage-census.md),
regenerated by [../vintage/tools/re/coverage_census.py](../vintage/tools/re/coverage_census.py).
A function is application logic iff it references a string **or** calls the
transport; the union of those two mechanical censuses is the complete surface (**53
functions**). Every command the client can send to the server is covered:

> `P` show · `D` dir · `L` link · `V` vote · `X` extend-life · `U` put/mail ·
> `ACCOUNT` · `Z` login · DAT/COM tokens `0x22`/`0x43` · ack `@`

(An earlier hand-picked module list silently omitted VOTE, LIFE-extend, PUT, MAIL,
LINK, ACCOUNT and the download flow-control sub-protocol — the census caught them.)

## Status

| Module | File | State |
|--------|------|-------|
| Shared types/decls | `compunet.h` | complete |
| Startup / main | `startup.c` | entry point (runtime startup supplied by toolchain) |
| Shared globals | `globals.c` | complete |
| Transport (cnet.device serial IO) | `transport.c` | **reconstructed** (serial_read/write/io_c, send_dat_packet) |
| Connect / transport bring-up | `connect.c` | **reconstructed** (open_transport) |
| Login / dial | `login.c` | **reconstructed** (do_connect, send_login_record, wait_connect_handshake, validate_login) |
| Frame display (PETSCII) | `frame.c` | **reconstructed** (render_char, build_font, blit_char_cell, RLE) |
| Navigation | `navigate.c` | **reconstructed** (goto_page, link_follow/goto, set_connection_state) |
| Directory actions | `directory.c` | **reconstructed** (vote, extend_life, account, put_frame) |
| File transfer | `transfer.c` | **reconstructed** (download_check, download_receive, upload_file) |
| Mail | `mail.c` | **reconstructed** (mail_submit, mail_read, upload/ID modes) |
| Resource tracking | `resources.c` | **reconstructed** (SAS/C mark/commit/cleanup) |
| Config | `config.c` | **reconstructed** (load_config, save_config_file) |
| Launch | `launch.c` | **reconstructed** (launch_editor, launch_tty) |
| UI / DOS / modem glue | `stubs.c` | **linkable stubs** — pending reconstruction |

Each reconstructed function is cross-checked field-by-field against
`recon_annotated.c` (struct offsets → real Amiga struct fields; `(**(base-LVO))()`
→ real OS prototypes). `frame.c`'s 0x00-0x1F / 0x80-0x9F PETSCII control tables are
best-effort with `TODO` markers where the jump-table entries are unconfirmed — see
[../vintage/tools/re/petscii-frame-format.md](../vintage/tools/re/petscii-frame-format.md).

## Building (vbcc, Kickstart 1.3 target)

The reconstruction compiles **and links into a whole HUNK executable** with
**vbcc** using the KS1.3 NDK headers. vbcc is not committed — see
[../vintage/tools/re/toolchain.md](../vintage/tools/re/toolchain.md) to build it.
Then:

```
VBCC=/path/to/vbcc ./build.sh
```

This compiles all 14 modules and links them with `amiga.lib` into
`compunet-client` — a genuine Amiga HUNK executable (magic `0x000003f3`, ~18 KB).
Only harmless `#endif !FOO` warnings appear (from the vintage 1.3 headers).

**What "links" proves and doesn't.** The 53 census **application** functions are
faithfully reconstructed. The UI (Intuition window/gadget/menu), DOS file I/O, and
modem-script layer below them is currently linkable **stubs** (`stubs.c`) so the
program links and the application logic can be exercised; replacing each stub with
its real reconstruction (from the recon addresses noted in `stubs.c`) is the
remaining work.

**Struct-offset verification.** `struct CnetRequest` field offsets are asserted at
compile time (via `offsetof`) to match the decompiled reference exactly:
`io_Command`=0x1c, `io_Error`=0x1f, `io_Actual`=0x20, `io_Length`=0x24,
`io_Data`=0x28, `io_Device`=0x14, `io_Offset`=0x2c. So the reconstruction reads/writes
the same bytes the original did — faithfulness is proven, not assumed.

SAS/C note: the source targets standard Amiga includes, so a `SMakefile` for real
SAS/C is a drop-in later; vbcc is the modern-host proxy for the compile/verify loop.
