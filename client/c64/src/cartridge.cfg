# Linker configuration for Compunet ROM code ($8000+)
# Frame data segment is pinned to $9507 (referenced by terminal code)

MEMORY {
    ROM: start = $8000, size = $2800, type = ro, fill = yes, fillval = $00;
}

SEGMENTS {
    HEADER:    load = ROM, type = ro, start = $8000;
    CODE:      load = ROM, type = ro;
    FRAMEDATA: load = ROM, type = ro, start = $9507;
    POSTFRAME: load = ROM, type = ro;
}
