# Linker configuration for 8K cartridge ROM at $8000-$9FFF
# EXROM=0, GAME=1 mode

MEMORY {
    ROM: start = $8000, size = $2000, type = ro, fill = yes, fillval = $FF;
}

SEGMENTS {
    HEADER:   load = ROM, type = ro, start = $8000;
    CODE:     load = ROM, type = ro;
}
