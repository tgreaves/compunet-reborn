# Linker configuration for ACIA driver (placed after terminal code at $BE03)

MEMORY {
    ACIA: start = $BE03, size = $0800, type = ro, fill = no;
}

SEGMENTS {
    ACIA: load = ACIA, type = ro, start = $BE03;
}
