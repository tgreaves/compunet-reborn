# Linker configuration for ACIA driver ($C800-$CFFF, RAM)
# This code is copied from ROM to RAM at boot by COLD_START

MEMORY {
    ACIARAM: start = $C800, size = $0800, type = ro, fill = yes, fillval = $00;
}

SEGMENTS {
    ACIADRV: load = ACIARAM, type = ro, start = $C800;
}
